# Godot Integration

Planned adapter: C++ GDExtension wrapper over Driveform Core/C ABI plus a Godot physics backend.

This directory remains a placeholder until the first cross-engine vertical slice is ready. Godot/Jolt integration must consume the same vehicle definition and solver behavior rather than introducing a second implementation.
