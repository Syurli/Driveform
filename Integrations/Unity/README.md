# Unity Integration

Planned adapter: C# wrapper over the stable native C ABI plus a Unity physics backend.

This directory remains a placeholder until the Core/Jolt/Unreal vertical slice is stable. Unity-specific vehicle dynamics must not fork the common Core behavior.
